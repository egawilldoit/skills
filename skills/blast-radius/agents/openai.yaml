interface:
  display_name: "Blast Radius"
  short_description: "Find what a change breaks elsewhere before it ships"
  default_prompt: "Use $blast-radius to find what this change could break elsewhere before it ships."
