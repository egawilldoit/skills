interface:
  display_name: "Model the Domain"
  short_description: "Encode domain rules in types, not conditionals"
  default_prompt: "Use $principle-model-the-domain to replace these scattered branches."
