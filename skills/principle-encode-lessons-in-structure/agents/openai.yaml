interface:
  display_name: "Encode Lessons in Structure"
  short_description: "Turn a repeated rule into a check, not more text"
  default_prompt: "Use $principle-encode-lessons-in-structure to encode this rule as a lint."
