interface:
  display_name: "Build the Lever"
  short_description: "Build the tool that does or proves the work"
  default_prompt: "Use $principle-build-the-lever to script this migration instead of editing by hand."
