interface:
  display_name: "Make Operations Idempotent"
  short_description: "Make retried steps converge to one end state"
  default_prompt: "Use $principle-make-operations-idempotent when designing this retry loop."
